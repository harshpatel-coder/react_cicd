version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - node -v
      - npm -v

  pre_build:
    commands:
      - npm install

  build:
    commands:
      - npm run build

artifacts:
  base-directory: build
  files:
    - '**/*'
