version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 23
    commands:
      - npm -f install
  build:
    commands:
      - npm run build
      - cp appspec.yml dist/
      - cp start_server.sh dist/
artifacts:
  files:
    - '**/*'
  base-directory: dist
